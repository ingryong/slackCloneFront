import Workspace from '@layouts/Workspace';
import React from 'react';

const Channel = () => {
  return (
    <Workspace>
      <div>워크스페이스 진입 완료</div>
    </Workspace>
  );
};

export default Channel;
